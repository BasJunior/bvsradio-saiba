"use client";
import { useCallback, useEffect, useState } from "react";
import type { AppSurface } from "@/components/app-vnext/AppBootstrap";
import { useAppSession } from "@/components/app-vnext/AppSessionProvider";
import { listOffline, offlineLicenseValid, offlineMediaAvailable, removeOffline, renewOffline, type OfflineItem, type OfflineManifest } from "@/lib/app-offline-native";

export default function AppOfflineDownloads({surface}:{surface:AppSurface}){const{token,signedIn}=useAppSession();const[items,setItems]=useState<OfflineItem[]>([]),[loading,setLoading]=useState(true),[notice,setNotice]=useState("");const refresh=useCallback(async()=>{setItems(await listOffline());setLoading(false)},[]);useEffect(()=>{void refresh();const sync=()=>void refresh();window.addEventListener("bvs:offline-change",sync);return()=>window.removeEventListener("bvs:offline-change",sync)},[refresh]);async function remove(trackId:string){await removeOffline(trackId);setNotice("Download removed from this device.");await refresh()}async function renew(item:OfflineItem){if(!token)return;setNotice("Checking rights…");const response=await fetch(`/api/app/offline/manifest?trackId=${encodeURIComponent(item.trackId)}&surface=${surface}`,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"}),payload=await response.json().catch(()=>({})) as {manifest?:OfflineManifest;error?:string};if(!response.ok||!payload.manifest){setNotice(payload.error||"Rights could not be renewed.");return}await renewOffline(payload.manifest);setNotice("Offline rights renewed.");await refresh()}
  if(!signedIn)return null;return <section className="mt-9"><p className="text-xs uppercase tracking-[.18em] text-brand">Offline</p><h2 className="mt-1 text-2xl font-semibold">Downloads on this device</h2><p className="mt-2 max-w-2xl text-sm text-text-secondary">Only recordings with explicit {surface === "ios" ? "iOS" : "Android"} clearance can be stored. Files stay inside BVS and periodically revalidate their rights.</p>{!offlineMediaAvailable()?<div className="mt-4 rounded-2xl border border-white/10 bg-white/[.025] p-4 text-sm text-text-secondary">Offline storage activates in the installed BVS app. Web previews never pretend a file is downloaded.</div>:loading?<div className="mt-4 h-20 animate-pulse rounded-2xl bg-white/[.04]"/>:items.length?<div className="mt-4 space-y-2">{items.map(item=>{const valid=offlineLicenseValid(item);return <article key={item.trackId} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/10 bg-white/[.025] p-4"><div className="min-w-0"><h3 className="truncate font-semibold">{item.title}</h3><p className="truncate text-sm text-text-secondary">{item.artist}</p><p className={`mt-1 text-xs ${valid?"text-emerald-200":"text-amber-200"}`}>{valid?`Available offline until ${new Date(item.licenseValidUntil).toLocaleDateString()}`:"Rights need revalidation before offline playback."}</p></div><div className="flex gap-2">{!valid?<button type="button" onClick={()=>void renew(item)} className="min-h-10 rounded-full border border-brand/40 px-3 text-xs font-semibold text-brand">Renew</button>:null}<button type="button" onClick={()=>void remove(item.trackId)} className="min-h-10 rounded-full border border-white/10 px-3 text-xs text-text-secondary">Remove</button></div></article>})}</div>:<div className="mt-4 rounded-2xl border border-dashed border-white/15 p-6 text-center text-sm text-text-secondary">No offline recordings yet. Cleared tracks show a Download action in Explore.</div>}{notice?<p role="status" className="mt-3 text-xs text-brand">{notice}</p>:null}</section>}
