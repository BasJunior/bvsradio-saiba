import Link from "next/link";import{notFound}from"next/navigation";
export default async function AppRoomsPage({params}:{params:Promise<{surface:string}>}){const{surface}=await params;if(surface!=="ios"&&surface!=="android")notFound();return <div className="mx-auto max-w-4xl px-4 pb-12 pt-6 sm:px-6"><header><p className="text-xs font-semibold uppercase tracking-[.2em] text-brand">BVS Rooms</p><h1 className="mt-2 text-4xl font-semibold tracking-tight sm:text-5xl">Listen together, safely.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-text-secondary">Signed-in spaces around BVS broadcasts and shows, with public-contact filtering, reporting and account blocking.</p></header><section className="mt-7"><Link href={`/app/${surface}/rooms/bvs-live`} className="block rounded-[1.75rem] border border-brand/25 bg-brand/[.08] p-5"><p className="text-xs uppercase tracking-[.18em] text-brand">Always available</p><h2 className="mt-2 text-2xl font-semibold">BVS Live Room</h2><p className="mt-2 text-sm text-text-secondary">The shared conversation around the main BVS broadcast.</p><span className="mt-4 inline-flex text-sm font-semibold text-brand">Enter Room →</span></Link></section><section className="mt-6 rounded-[1.5rem] border border-white/10 bg-white/[.025] p-5"><h2 className="font-semibold">Room safety</h2><p className="mt-2 text-sm leading-6 text-text-secondary">Keep phone numbers, email addresses and messaging-app handles private. Use Safety on any message to report it or hide that account immediately.</p></section></div>}
