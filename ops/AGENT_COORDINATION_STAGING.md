# Agent coordination — staging (2026-08-20)

**Discord agent:** Flow v2 handoff completed through `c653bb5`.

**Telegram/Saiba:** owns the post-handoff beta implementation and **Vercel project `bvsradio-beta`**. Does **not** touch `bvsradio-saiba` / `bvsradio.com` prod.

**Shared rules:**
- Default target = staging (`bvsradio-beta`), never prod during Apple lock
- Product north star = ChatGPT **bvsradio production**
- Producer money loop still Phase B after staging URL is live
