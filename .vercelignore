# Keep Vercel Fluid/VHS packaging under the builder disk limit.
# Site needs public/ + src/ + package files only.

# Native / store packaging (not served by Next.js)
android
ios
ops
artifacts
out
output
data

# Local / tooling
.next
node_modules
.git
.vercel
*.tsbuildinfo
tsconfig.tsbuildinfo

# Large store binaries
**/*.apk
**/*.aab
**/*.ipa
**/*.zip

# Docs / blueprints not needed at runtime
monako-glass-blueprint.md
COMMERCE.md
MOBILE.md
DEPLOYMENT_SECURITY.md
supabase-*.sql

# C02 keeps its small migration snapshot in the build context so test:song-workspace
# can enforce database security invariants. Next.js does not ship this file as a route.
!supabase-song-workspaces.sql

# Env samples (secrets never go to Vercel via git; avoid clutter)
.env*
!.env.local.example
